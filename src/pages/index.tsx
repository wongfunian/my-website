import { NextPage } from 'next';
import { Button } from '@nextui-org/react';

const Home: NextPage = () => {
    return (
        <div>
            <Button>Click me</Button>
        </div>
    );
};

export default Home;
